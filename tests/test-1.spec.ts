import { test, expect } from '@playwright/test';

/*test.only('test', async ({ page }) => {
  await page.goto('https://rahulshettyacademy.com/loginpagePractise/');
  await page.getByRole('button', { name: 'Odrzuć wszystko' }).click();
  await page.getByRole('combobox', { name: 'Szukaj' }).click();
  await page.getByRole('combobox', { name: 'Szukaj' }).fill('palywright');
  await page.goto('https://www.google.com/');
  await page.getByRole('combobox', { name: 'Szukaj' }).click();
  await page.getByRole('combobox', { name: 'Szukaj' }).fill('playwright');
  await page.getByText('documentation').click();
  await page.locator('iframe[name="a-xei4tpooze9a"]').contentFrame().getByRole('checkbox', { name: 'I\'m not a robot' }).click();
  await page.locator('iframe[name="c-xei4tpooze9a"]').contentFrame().getByText('Select all images with bridges').click();
  await page.locator('iframe[name="c-xei4tpooze9a"]').contentFrame().locator('.rc-imageselect-tile').first().click();
  await page.locator('iframe[name="c-xei4tpooze9a"]').contentFrame().locator('tr:nth-child(3) > td').first().click();
  await page.locator('iframe[name="c-xei4tpooze9a"]').contentFrame().locator('tr:nth-child(3) > td:nth-child(3)').click();
  await page.locator('iframe[name="c-xei4tpooze9a"]').contentFrame().getByRole('button', { name: 'Verify' }).click();
}); */